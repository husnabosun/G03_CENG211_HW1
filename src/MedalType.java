/**
 * MedalType Enum Contract:
 * Responsibility: To define and enforce the discrete set of possible medal awards
 * that can be assigned to gamers based on their total season score.
 */
public enum MedalType {
    GOLD, SILVER, BRONZE, NONE
}
